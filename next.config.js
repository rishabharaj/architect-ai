/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Vercel-compatible defaults
  output: undefined, // Uses default Vercel output
};

module.exports = nextConfig;
